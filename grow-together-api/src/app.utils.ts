
const PASSWORD_REG = /^(?=.*?[A-Z])(?=.*?[a-z])(?=.*?[0-9])(?=.*?[#?!@$%^&*-]).{8,}$/;
const PASSWORD_MSG = "Password is incorrect";
export const REGEX = {
    PASSWORD_REG
}

export const MESSAGES = {
    PASSWORD_MSG
}